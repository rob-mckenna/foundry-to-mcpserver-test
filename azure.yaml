# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: foundry-mcp-server
metadata:
  template: foundry-mcp-server@1.0.0
services:
  mcp-server:
    project: ./mcp-server
    language: js
    host: containerapp
